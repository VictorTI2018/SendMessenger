import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    container: {
        flex: 1,
        padding: 10,
    },
    formContainer: {
        flex: 10,
        marginBottom: 10,
        justifyContent: 'center'
    },

    link: {
        fontSize: 17,
        textAlign: 'center',
        color: '#FFF'
    },
    buttonContainer: {
        flex: 2
    }
})