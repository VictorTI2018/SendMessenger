import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    container: {
        marginTop: 20
    },

    containerTitle: {
        flexDirection: 'row',
        padding: 10,
        borderWidth: 1,
        borderRadius: 10,
        marginLeft: 40,
        marginRight: 40,
        marginBottom: 50
    },
    title : {
        fontSize: 15,
        fontWeight: 'bold',
        textAlign: 'center',
        marginBottom: 10,
    }
})